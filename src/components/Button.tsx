import React from "react";
import Icon from "../components/Icon";

const Button: React.FC = () => {
  return (
    <button>
      <Icon nameIcon="fly" />
      Connexion
    </button>
  );
};

export default Button;
